We can split the data types into three main categories:

Numerical
Categorical
Ordinal
Numerical data are numbers, and can be split into two numerical categories:

Discrete Data
- counted data that are limited to integers. Example: The number of cars passing by.
Continuous Data
- measured data that can be any number. Example: The price of an item, or the size of an item
Categorical data are values that cannot be measured up against each other. Example: a color value, or any yes/no values.

Ordinal data are like categorical data, but can be measured up against each other. Example: school grades where A is better than B and so on.

By knowing the data type of your data source, you will be able to know what technique to use when analyzing them.

You will learn more about statistics and analyzing data in the next chapters.

import numpy

speed = [99,86,87,88,111,86,103,87,94,78,77,85,86]
x = numpy.mean(speed)# geeft gemiddelde
x = numpy.median(speed) # geeft getal in het midden van de gesorteerde rij, als dat 2 waarden zijn dan het midden van die 2 waarden
x = stats.mode(speed) # meest voorkomende waarde in de array
x = numpy.std(speed) # geeft de deviation, dus een waarde die aangeeft hoe gespreid de waarden in de aray zijn van het gemiddelde, 
als het allemaal dicht bij het gemiddelde is .
stel je voor x = 0.9 dan Meaning that most of the values are within the range of 0.9 from the mean value, which is 86.4.
####
Percentiles are used in statistics to give you a number that describes the value that a given percent of the values are lower than.
ages = [5,31,43,48,50,41,7,11,15,39,80,82,32,2,8,6,25,36,27,61,31]
What is the 75. percentile? The answer is 43, meaning that 75% of the people are 43 or younger.

The NumPy module has a method for finding the specified percentile:
x = numpy.percentile(ages, 75) # geeft 43 , dus 75% is lager of gelijk aan 43
###
als je een grote data set wil kan je die zelf maken:
x = numpy.random.uniform(0.0, 5.0, 250) # geeft een array met 250 waarden, willekeurig tussen 0 en 5

x = numpy.random.normal(5.0, 1.0, 100000) # gem, standaardafwijking, aantal waarden ( array.size)
##
import matplotlib.pyplot as plt

x = [5,7,8,7,2,17,2,9,4,11,12,9,6]
y = [99,86,87,88,111,86,103,87,94,78,77,85,86]

plt.scatter(x, y)
plt.show()
# geeft een x,y diagram 

als je willekeurige data nodig hebt dan kan je dat ook zelf maken:
x = numpy.random.normal(5.0, 1.0, 1000)
y = numpy.random.normal(10.0, 2.0, 1000)
veranderd het gemiddelde en standaardafwijking zodat de waarden meer willekeurig zijn
##
liniare regressie, wat ik heb gehad bij wiskunde D, dat een groep punten een linear verband kunnen hebben
######
import matplotlib.pyplot as plt
from scipy import stats

x = [5,7,8,7,2,17,2,9,4,11,12,9,6]
y = [99,86,87,88,111,86,103,87,94,78,77,85,86]

slope, intercept, r, p, std_err = stats.linregress(x, y)

def myfunc(x):
  return slope * x + intercept

mymodel = list(map(myfunc, x))

plt.scatter(x, y)
plt.plot(x, mymodel)
plt.show()
######

If your data points clearly will not fit a linear regression (a straight line through all data points), it might be ideal for polynomial regression.

Polynomial regression, like linear regression, uses the relationship between the variables x and y to find the best way to draw a line through the data points.
#polynomial als in een kromme
import numpy
import matplotlib.pyplot as plt

x = [1,2,3,5,6,7,8,9,10,12,13,14,15,16,18,19,21,22]
y = [100,90,80,60,60,55,60,65,70,70,75,76,78,79,90,99,99,100]

mymodel = numpy.poly1d(numpy.polyfit(x, y, 3))

myline = numpy.linspace(1, 22, 100)

plt.scatter(x, y)
plt.plot(myline, mymodel(myline))
plt.show()
##
Create the arrays that represent the values of the x and y axis:

x = [1,2,3,5,6,7,8,9,10,12,13,14,15,16,18,19,21,22]
y = [100,90,80,60,60,55,60,65,70,70,75,76,78,79,90,99,99,100]

NumPy has a method that lets us make a polynomial model:

mymodel = numpy.poly1d(numpy.polyfit(x, y, 3))

Then specify how the line will display, we start at position 1, and end at position 22:

myline = numpy.linspace(1, 22, 100)

Draw the original scatter plot:

plt.scatter(x, y)

Draw the line of polynomial regression:

plt.plot(myline, mymodel(myline))

Display the diagram:

plt.show()
##
x = [1,2,3,5,6,7,8,9,10,12,13,14,15,16,18,19,21,22]
y = [100,90,80,60,60,55,60,65,70,70,75,76,78,79,90,99,99,100]

mymodel = numpy.poly1d(numpy.polyfit(x, y, 3))

print(r2_score(y, mymodel(x))) r-squared
# geeft aan hoe goed de gegeven kromme overeenkomt met de data, van 0 tot 1

mymodel = numpy.poly1d(numpy.polyfit(x, y, 3))

speed = mymodel(17)
print(speed)
##
oke dus stel je voor dat je een csv hebt dan doe je dit:

import pandas as pd 
from sklearn import linear_model
df = pd.read_csv("data.csv")
X = df[['Weight', 'Volume']]
y = df['CO2']
dus ik kan straks met weight, volume voorspellen wat de CO2 uitstoot is

regr = linear_model.LinearRegression()
regr.fit(X,y) #nu is de lineare model aangepast op onze data
predictedCO2 = regr.predict([[2300, 1300]]) 
print(regr.coef_) # coef_ staat voor coefficient en het geeft aan dat wanneer variable X met 
+1 wordt aangepast, hoeveel wordt co2 aangepast (voorspelling)
dus dit geeft [0.00755095 0.00780526] , en index =0 staat voor wanneer weight wordt aangepast +1 en index=1 als de volume
wordt aangepast met +1 

#als je een dataframe hebt en de waarde zitten op hele andere groten, dan wil je vaak dat ze in dezelfde grote zitten
dus in de duizenden, of in de eenheden, zodat je ze makkelijk kan vergelijken voor een lineare regressie
dus dat moet je soms aanpassen, bijvoorbeeld als een volume in liters is maar je wil het in cm^3 zodat de grootte overeenkomt 
met het gewicht (ook in de duizend)
dit doe je blijkbaar met 
from sklearn.preprocessing import StandardScaler
scale = StandardScaler()
scaledX = scale.fit_transform(X)
###


mymodel = numpy.poly1d(numpy.poAlllyfit(train_x, train_y, 4))

myline = numpy.linspace(0, 6, 100)
myline = numpy.linspace(0, 6, 100) # geeft een vloeiende lijn van 100 punten van 0 naar 6
mymodel = numpy.poly1d(numpy.polyfit(train_x, train_y, 4)) # geeft een kromme van de 4de graads x^4

plt.scatter(train_x, train_y) # geeft alle punten 
plt.plot(myline, mymodel(myline)) # zet de lijn er in, x,y mymodel(myline) is de y zegmaar

plt.show() # laat het zien

r2 = r2_score(train_y, mymodel(train_x)) # geeft r^2 , in andere woorden wat is de relationship tussen x en y waarde, 1 is super goed 0 is slecht
print(mymodel(5)) geeft y waarde
