import pandas as pd

mydataset = {
  'cars': ["BMW", "Volvo", "Ford"],
  'passings': [3, 7, 2]
}

myvar = pd.DataFrame(mydataset)

a = [1, 7, 2]

myvar = pd.Series(a) #Series altijd 1 demensionaal
myvar[0]  #1

a = [1, 7, 2]

myvar = pd.Series(a, index = ["x", "y", "z"]) # dit betekent dat de rijen de waarden x,y en z krijgen ipv de index 0,1 en 2
print(myvar["y"])


calories = {"day1": 420, "day2": 380, "day3": 390}

myvar = pd.Series(calories)
geeft: 
day1    420
day2    380
day3    390
dtype: int64

data = {
  "calories": [420, 380, 390],
  "duration": [50, 40, 45]
} #dit is een dictionary, en verander je naar een dataframe om het in pandas te gebruiken

myvar = pd.DataFrame(data)

geeft:
   calories  duration
0       420        50
1       380        40
2       390        45

df.loc[0]# geeft eerste RIJ
df.loc[[0,1]] # geeft rij 1 en 2


data = {
  "calories": [420, 380, 390],
  "duration": [50, 40, 45]
}

df = pd.DataFrame(data, index = ["day1", "day2", "day3"]) 
# dus die 2 keys in de dictionary zijn de kolommen en de size van de array van de keys
zijn de aantal rijen, en in .DataFrame staat de index voor de rij en geef je elke rij een naam 
ipv normale indexen 0,1,2

        calories  duration
  day1       420        50
  day2       380        40
  day3       390        45

print(df.loc["day2"]) # je kan dus die indexen gebruiken, het is wel een string
 calories    380
  duration     40
  Name: day2, dtype: int64

df = pd.read_csv('data.csv')
print(df.to_string()) # print hem zo, omdat dit een string is en minder data kost, niet zwaar

If you have a large DataFrame with many rows, Pandas will only return the first 5 rows, and the last 5 rows:


pd.options.display.max_rows = 9999 # als je alle rows wil zien ipv verkorte versie

df = pd.read_csv('data.csv') 

JSON is plain text, but has the format of an object, and is well known in the world of programming, including Pandas.
df = pd.read_json('data.json')

print(df.to_string()) 

data = {
  "Duration":{
    "0":60,
    "1":60,
    "2":60,
    "3":45,
    "4":45,
    "5":60
  },
  "Pulse":{
    "0":110,
    "1":117,
    "2":103,
    "3":109,
    "4":117,
    "5":102
  },
  "Maxpulse":{
    "0":130,
    "1":145,
    "2":135,
    "3":175,
    "4":148,
    "5":127
  },
  "Calories":{
    "0":409,
    "1":479,
    "2":340,
    "3":282,
    "4":406,
    "5":300
  }
}

df = pd.DataFrame(data)

print(df) 

print(df.head(10)) # geeft de eerste 10 rijen
print(df.tail())  #geeft automatisch laatste 5

print(df.info())  # geeft informatie over de data

  <class 'pandas.core.frame.DataFrame'>
  RangeIndex: 169 entries, 0 to 168
  Data columns (total 4 columns):
   #   Column    Non-Null Count  Dtype  
  ---  ------    --------------  -----  
   0   Duration  169 non-null    int64  
   1   Pulse     169 non-null    int64  
   2   Maxpulse  169 non-null    int64  
   3   Calories  164 non-null    float64
  dtypes: float64(1), int64(3)
  memory usage: 5.4 KB
  None
The info() method also tells us how many Non-Null values
 there are present in each column, and in our data set it 
seems like there are 164 of 169 Non-Null values in the "Calories" column.


Data cleaning means fixing bad data in your data set.

Bad data could be:

Empty cells NaN
Data in wrong format dus geen string ofzo
Wrong data 
Duplicates


new_df = df.dropna() # haalt alle rijen weg die een lege vak hebben (NaN) en geeft nieuwe dataframe
print(new_df.to_string()) # altijd als je fd print breng naar string voor datacomsumptie

df.dropna(inplace = True) # veranderd huidige dataFrame
df.dropna(subset=['Date'], inplace = True)

df = pd.read_csv('data.csv')

df.fillna(130, inplace = True) # dus vult elke lege vak met waarden 130

df.fillna({"Calories": 130}, inplace=True) #vult alle lege vakken in het kolom calories met 130

x = df["Calories"].mean()  #GEMIDDELDE

df.fillna({"Calories": x}, inplace=True)


x = df["Calories"].median() #mediaan, het middengetal in  de gesorteerde getallen

df.fillna({"Calories": x}, inplace=True)

x = df["Calories"].mode()[0] # geeft de meest terugkomende waarde in calories

df.fillna({"Calories": x}, inplace=True)

Let's try to convert all cells in the 'Date' column into dates.

Pandas has a to_datetime() method for this:

df['Date'] = pd.to_datetime(df['Date'], format='mixed') # om een hele kolom dezelfde datatype te geven, bij datum doe je dit

gave us a NaT value, which can be handled as a NULL value, and we can remove 
the row by using the dropna() method.


df.dropna(subset=['Date'], inplace = True) # haal de rij weg waarbij er een lege vakje is bij date alleen


df.loc[7, 'Duration'] = 45 # als je een specifieke waarde wil veranderen

df.loc[7, 'Duration'] = 45 # row (nummer), colomn ( ky

for x in df.index:
  if df.loc[x, "Duration"] > 120:
    df.loc[x, "Duration"] = 120


for x in df.index:
  if df.loc[x, "Duration"] > 120:
    df.drop(x, inplace = True)


df.drop_duplicates(inplace = True)

Remember: The (inplace = True) will make sure that the method does NOT return a new DataFrame, but it will remove all duplicates from the original DataFrame.


A great aspect of the Pandas module is the corr() method.

The corr() method calculates the relationship between each column in your data set.


df.corr()
Note: The corr() method ignores "not numeric" columns.


1 means that there is a 1 to 1 relationship (a perfect correlation), and for this data set, each time a value went up in the first column, the other one went up as well.

0.9 is also a good relationship, and if you increase one value, the other will probably increase as well.

-0.9 would be just as good relationship as 0.9, but if you increase one value, the other will probably go down.

0.2 means NOT a good relationship, meaning that if one value goes up does not mean that the other will.

What is a good correlation? It depends on the use, but I think it is safe to say you have to have at least 0.6 (or -0.6) to call it a good correlation.

We can see that "Duration" and "Duration" got the number 1.000000, which makes sense, each column always has a perfect relationship with itself.
##
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv('data.csv')

df.plot()

plt.show()

# IK HEB IN FAVOURITE een dataframe reference die allemaal andere functies geeft in pandas met dataframes
print(df.keys()) # geeft kolommen
df.add(15) # doet +15 bij alle waarden in dataframe























